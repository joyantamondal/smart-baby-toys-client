import { Box } from '@mui/system';
import React from 'react';

const Pay = () => {
    return (
        <Box style={{display:'flex', justifyContent:'center'}}>
                <h2 style={{color:'red'}}>Payment System is Underconstruction!</h2>
            </Box>
    );
};

export default Pay;